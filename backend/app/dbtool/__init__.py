"""Database lifecycle tooling: migrate, seed, reset, verify and generate docs.

Run from the backend directory with ``uv run python -m app.dbtool <command>``, or from the
repo root with the ``npm run db:*`` scripts. See docs/database/README.md.
"""
